{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Snake Game",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/snake",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "externalConsole": true,
            "MIMode": "lldb",
            "preLaunchTask": "make"
        }
    ]
}
